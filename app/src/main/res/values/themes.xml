<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BayanAni" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.BayanAni" parent="Base.Theme.BayanAni">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="primaryBtn" parent="Theme.BayanAni">
        <item name="backgroundTint">@color/lgt_prm_green</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="secondaryBtn" parent="primaryBtn">
        <item name="backgroundTint">@color/drk_scd_navyblue</item>
    </style>

    <style name="tertiaryBtn" parent="primaryBtn">
        <item name="backgroundTint">@color/drk_prm_green</item>
    </style>

</resources>